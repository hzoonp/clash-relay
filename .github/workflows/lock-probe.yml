name: P50 lock probe

on:
  pull_request:
    paths:
      - "requirements.lock"
      - "requirements-dev.lock"
      - ".github/workflows/lock-probe.yml"

permissions:
  contents: read

jobs:
  probe:
    name: Wheel hashes Python ${{ matrix.python }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: ${{ matrix.python }}
      - name: Resolve compatible binary wheels and print immutable hashes
        run: |
          set -euo pipefail
          mkdir -p .work/wheels
          python -m pip download \
            --only-binary=:all: \
            --dest .work/wheels \
            --requirement requirements-dev.lock
          python - <<'PY'
          import hashlib
          from pathlib import Path

          for path in sorted(Path('.work/wheels').iterdir(), key=lambda item: item.name.lower()):
              digest = hashlib.sha256(path.read_bytes()).hexdigest()
              print(f"P50_WHEEL {path.name} sha256:{digest}")
          PY
