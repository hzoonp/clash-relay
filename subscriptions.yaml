version: 1

# No URL belongs in this tracked file. Real subscription URLs are resolved from
# CLASH_RELAY_SUBSCRIPTIONS. Production now uses one general node inventory and
# lets ACL4SSR perform all service/media/application routing.
subscriptions:
  - id: subscription_1
    display_name: 订阅源 1
    enabled: true
    required: false
    secret_name: SUBSCRIPTION_1_URL
    priority: 100
    on_error: skip
    allowed_uses: [general]
    allowed_countries: [OTHER]
    default_capabilities: [general]
    default_cost_level: standard

  - id: subscription_2
    display_name: 订阅源 2
    enabled: true
    required: false
    secret_name: SUBSCRIPTION_2_URL
    priority: 200
    on_error: skip
    allowed_uses: [general]
    allowed_countries: [OTHER]
    default_capabilities: [general]
    default_cost_level: standard

  - id: subscription_3
    display_name: 订阅源 3
    enabled: true
    required: false
    secret_name: SUBSCRIPTION_3_URL
    priority: 300
    on_error: skip
    allowed_uses: [general]
    allowed_countries: [OTHER]
    default_capabilities: [general]
    default_cost_level: standard

  - id: subscription_4
    display_name: 订阅源 4
    enabled: true
    required: false
    secret_name: SUBSCRIPTION_4_URL
    priority: 400
    on_error: skip
    allowed_uses: [general]
    allowed_countries: [OTHER]
    default_capabilities: [general]
    default_cost_level: standard
